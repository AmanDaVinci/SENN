{
  "exp_name": "Dummy",
  "conceptizer": "ConvConceptizer",
  "parameterizer": "ConvParameterizer",
  "cl_sizes": [1, 10, 20],
  "kernel_size": 3,
  "hidden_sizes": [40],
  "dropout": 0.5,
  "aggregator": "SumAggregator",
  "device": "cuda:0",
  "num_classes": 10,
  "lr": "1e-5",
  "checkpoint_dir": "./saved_checkpoints/",
  "load_checkpoint": "delete this line if not loading from checkpoint",
  "epochs": 5,
  "robust_reg": 0.1,
  "concept_reg": 1e-3,
  "print_freq": 100,
  "x_dim": "I think this isn't needed if we rewrite validate function",
  "sample_size": "I think this isn't needed if we delete the sample function",
  "image_dir": "can be changed to dir of the concept-images in case of an image dataset",
  "checkpoint_file": "dummy_mnist.cpt",
  "eval_freq" : 30
}
